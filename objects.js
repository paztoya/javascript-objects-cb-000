var playlist = {
  artistName: "songTitles"
}

function updatePlaylist(playlist, artistName, songTitles){
  object.assign({}, playlist, {artistName, songTitles})
}

function removeFromPlaylist(playlist, artistName){
  delete.artistName
}
